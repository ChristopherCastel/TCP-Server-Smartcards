/*
 * main.cpp
 *
 *  Created on: 28 Feb 2019
 *      Author: Castel Christopher
 */

#include "client/client_api.h"
#include "terminal/flyweight_factory.h"
#include "terminal/terminals/terminal_pcsc.h"

using namespace client;

int __cdecl main(void) {
	FlyweightFactory flyweight;
	ITerminalFactory pcsc = new TerminalPCSC();
	flyweight.addFactory("PCSC", new TerminalPCSC());


	ClientAPI client;
	client.initClient("./config/init.json");
	client.connectClient();
	return 0;
}
